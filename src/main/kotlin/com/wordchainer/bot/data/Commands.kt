package com.wordchainer.bot.data

import com.wordchainer.bot.commands.Command
import com.wordchainer.bot.commands.general.*

object Commands {
    val commands: MutableList<Command> = mutableListOf(
        Ping
    )
}